# GIFP 6.1.0 更新说明

跨帧稳定优化和 LZW 成本搜索已接入剪辑工作台及快速工具的 GIF 导出。GIFP 永久免费，本次 GitHub 发布为源码预览。

## 使用与行为

两个开关位于 GIF 的有损压缩选项中，均默认关闭。旧工程缺少设置时也保持关闭，
开启状态随工程保存并支持撤销重做。它们不会为取得收益而额外改变所选输出尺寸、
帧数、逐帧延时或循环设置；工程导出仍按既有画布与体积上限规则执行。

| 选项 | 当前产品行为 |
| --- | --- |
| 跨帧稳定优化 | 利用严格对齐的量化前画面，在保护条件内尝试复用相近颜色，减少静止区域的跨帧变化。 |
| LZW 成本搜索 | 在小幅颜色变化内搜索索引组合，以真实序列化后的 GIF 字节数判断收益。 |
| 智能无损优化 | 原有独立结构路线，联合优化局部画面、透明复用与处置方式，要求解码显示一致。 |

两条新路线属于**低误差有损**，不是“开启后画质一定无损”的选项。即使两者同时开启，
也分别从同一份不可变基线生成候选，再选择合格候选中更小的一个，不把两次有损结果
串联。新候选采用后跳过旧有损索引压缩；未采用时，用户已开启的旧索引压缩仍可按原规则
执行。新阶段的报告只描述本阶段，不能据此把后续处理后的整个成品称作无损。

## 画质参考与保护

优先参考完成剪辑、实际尺寸与帧率选择、裁剪、滤镜和字幕／贴纸叠加后的**量化前 RGB**。
这是当前输出处理链的参考画面，不是未处理原素材。工程工作台使用已经合成的无损 FFV1
时间线作为输入；快速工具重放最终胜出路线的滤镜和帧选择，按帧序严格核对，避免从 GIF
厘秒时间戳反推 30/60 FPS 原帧而发生错位。

| 报告中的参考类型 | 含义与回退 |
| --- | --- |
| `prequantized_source` | 已取得严格对齐的不透明量化前 RGB，允许在固定误差范围内评估新候选。 |
| `gif_only` | 本路线未提供可用的源参考；跳过跨帧稳定，LZW 仅接受独立解码后显示像素完全一致的变化。 |
| `unavailable` | 尝试提取的源参考无法严格对齐或不满足要求；同样跳过跨帧稳定，LZW 仅保留像素一致的回退可能。 |

仅 GIF 回退不证明相对源画面的画质。存在透明源画面、帧数或尺寸不匹配，或分段调色板的
独立时序无法严格重放时，不会用近似参考冒充对齐。

候选采用需要同时满足：

- 实际文件小于原 GIF，也小于由相同写入器和结构整理方式生成的像素一致对照；设置了
  字节上限时还须满足该上限。
- 独立 FFmpeg 解码与内部显示模拟一致，保留尺寸、帧数、延时、循环和像素宽高比等契约。
- 通过预先固定的颜色误差、强边缘、Alpha、低频颜色偏差、色带风险、静止区域跨帧变化
  及循环首尾接缝检查。色带指标是风险代理，不能替代真实观感检查。

## 搜索预算与失败处理

当前适用规模限制为输入 GIF 不超过 32 MiB、最多 240 帧、完整 RGBA 时间线不超过
96 MiB；搜索另有 512 MiB 估算内存、1,024 次压缩探测及 60 秒总时间预算，各阶段还受
独立预算限制。这些是当前新阶段的资源边界，不是整个软件的导出规格上限，也不保证
所有符合这些规格的素材都能完成搜索。

没有满足保护条件的修改、实际字节无收益、预算耗尽或候选验证失败时，本阶段保留
已验证的基线并报告原因。候选只在验证后原子替换，取消继续遵守现有任务取消与发布
规则。新增搜索可能增加导出等待时间。

## 收益口径与现有证据

报告分别记录原文件、候选文件和同写入器对照字节数。`algorithm_saved_bytes` 的口径是
“同写入器对照减去候选”，避免把更换写入器或结构整理的收益全部记为新算法收益。
未采用候选的数值仅为候选测量，不能计入最终成品节省。

本次机制用例测得原 GIF **2,131 字节**、同写入器对照 **1,780 字节**、候选 **1,004 字节**；
相对同写入器的算法增量为 **776 字节**。这是构造用例上的机制证据，用于确认新路线能
贡献独立于写入器变化的实际收益，不是代表性素材压缩率。普通实际导出没有合格收益时
会保留基线。

当前未完成固定素材集的完整竞品复赛与观感盲测，不据此宣称普遍收益、极限压缩或
第一梯队。

## 验证记录

6.1.0 实现的现有记录包含 156 项前端相关测试、46 项算法筛选测试及 4 项真实原生导出测试通过；静态检查与本地打包校验通过。305 个构建输入在同步前重新核对一致。

本次源码同步另从独立目录执行生产构建与前端相关回归；结果见 [发布记录](docs/release/6.1.0.md)。这些是定向验证，未代替全量回归或 Windows 桌面完整人工验收。
