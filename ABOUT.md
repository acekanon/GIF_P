# GIFP 6.1.0

- 作者：acekanon
- 软件性质：免费软件（freeware）
- 版本：`6.1.0`
- 当前发行状态：内部预览，未签名

GIFP 用于把视频、GIF、Animated WebP、APNG 等素材编辑并导出为聊天动图、
Animated AVIF、网页动图、Live Photo 或视频。快速生成按使用场景配置参数；精细编辑提供裁剪、
时间范围、删帧、调速、滤镜、文字和输出体积控制。

6.1.0 将跨帧稳定优化和 LZW 成本搜索接入剪辑工作台及快速工具的 GIF 导出，均默认关闭并标注为低误差有损。参考画面取自完成剪辑、缩放、滤镜与叠加后的量化前 RGB；没有严格对齐的不透明参考时跳过跨帧稳定，LZW 仅允许解码显示像素一致的回退。候选须同时小于原 GIF 和同写入器对照，并通过颜色、边缘、色带风险、跨帧变化、循环接缝与独立解码检查。无合格收益、预算不足或候选失败时保留基线；采用新候选后不叠加旧有损索引压缩。两条新路线的报告分别记录候选字节、同写入器对照及算法增量，实际收益以各次导出为准，未作普遍压缩收益或行业梯队声明。当前最终测试和发行包校验状态见 [6.1.0 说明](release-6.1.0.md)。

6.0.1 内部体验版支持字幕和贴纸跟随主轨删帧、复制、定格及片段长度调整；动态贴纸保存源时间与定格状态，锁定图层保持原位。新工程默认联动，旧工程保留绝对时段，开关随工程保存并支持撤销重做。6.0 工作台已有多素材主轨、单帧独立编辑、文字／贴纸叠加、基础关键帧和工程文件；原生合成后统一生成 GIF。结构搜索以可选智能无损优化接入实际导出，普通预览覆盖前 15 秒，当前帧可独立渲染。

5.7.27 将删帧升级为真实输出帧编辑：逐页读取精确缩略图，支持单选、Shift 连选、全选、撤销和恢复；新增“每 N 帧保留 1 帧”的手动抽帧，以及收紧时间线和保持总时长两种时序。播放、预览、导出时长与后端帧索引使用同一套映射，并修复 FFmpeg 9 下缩略图色彩范围与短素材尾帧对齐。

5.7.26 改进屏幕录制区域选择：启动桌面捕获前立即显示准备进度，选择任务移至独立工作线程；原生框选层采用双缓冲、限频局部重绘与复用绘图资源，降低多屏和高分辨率桌面上的等待感、拖动卡顿与闪烁。

5.7.25 新增智能体积导演与对象跟踪式标注、隐私处理，重构精细编辑输出侧栏：合并重复的体积与编码控制，只保留单次生成入口，将手动覆盖和完整报告下沉，并以紧凑的生成结果与质量指标完成交付反馈。

5.7.24 完成 FFmpeg 9 与 Animated WebP 升级的发行收口：统一产品、诊断、构建和便携包版本，补齐全量前后端、合规、格式与静态检查；同时修复非忙碌状态下操作反馈被装饰进度文案遮蔽的问题，完成态继续保持“已生成”的精简表达。

5.7.23 完善 Animated WebP 输出：压缩强度 0 改走 BGRA 可见像素与 Alpha 无损链，不再预先执行 4:2:0 色度抽样；有损输出按真实透明度选择 YUV420P 或 YUVA420P，透明度检测失败时保守保留 Alpha。界面新增 WebP 四档优化级别，后端映射到 libwebp method 2/4/5/6，并避免使用会重置无损与 method 的内容 preset；运行时能力门禁同时验证动画解复用器、解码器、编码器、像素格式与必要选项。

5.7.22 重做 Animated WebP 导入时序：通过常量内存 RIFF 扫描精确读取画布、帧数、循环与毫秒延时，长动画不再依赖 FFprobe 猜测或整文件载入；静态 WebP 保持普通图片解复用，只有确认的动画容器才启用 FFmpeg 9 `webp_anim` 有限循环与透明背景策略，并保留所有正数短帧延时。

5.7.21 将内部媒体运行时锁定到 FFmpeg 9.0 正式版：使用精确版本、固定下载地址与 SHA-256 的 Windows x64 GPL shared 构建，验证动画 WebP 解码、封装、编码和现有 DLL 主版本兼容性；公共 LGPL 构建链继续保持独立、默认关闭发布。

5.7.20 为画面裁剪补齐像素标尺：裁剪模式在素材上方和左侧显示基于源分辨率的主次刻度，标尺独占画布外缘不遮挡画面；操作区实时显示裁剪 X/Y 坐标与宽高像素，退出裁剪后自动释放标尺空间。

5.7.18 完成本轮界面减法：去除顶部核心流程组的浅绿色衬底与格式卡片中的方法说明；编码后评分改为“编码完成，正在打分中”并可单独终止；完成页只保留“已生成”和 VMAF、SSIM 两个实测质量章。

5.7.17 从产品界面移除真实平台交付验证入口与弹窗，保留底层开发验收门禁，不再向普通用户展示内部验证状态。

5.7.16 将精确体积改为用户直接指定最大 MB：该数值作为 `target_size_bytes` 硬上限，成品不得超过；移除 Fast 相对比例和额外基线编码，无法满足时明确显示不可达。

5.7.15 修复桌面头部在常见窗口尺寸下的挤压、截断和逐字换行；主导航回归快速生成、精细编辑、合并、录屏与表情包五个核心入口；移除动态海报入口和发行状态展示，平台验证与辅助设置改为紧凑控件。

5.7.14 将故障诊断改为严格白名单输出：默认复制内容不再读取原始错误，只包含受控版本、操作、分类、能力与资源字段；路径、文件名、网址和令牌等原始技术详情仅能由用户明确展开并在本地查看。

5.7.13 新增平台验证中心与安全可信摘要：集中展示 8 个交付面的实机状态、规则来源、回退格式和五步验证进度；严格 JSON 证据校验进入常规门禁；发行可信摘要只包含允许分享的结构化字段。

5.7.12 新增发行可信度闭环：应用内显示发行渠道、程序哈希和签名状态；正式 public 渠道必须使用白名单 Authenticode 签名且禁止 unsigned 覆盖；随包校验器复核完整文件集合、发行记录、程序哈希与实时签名。

5.7.11 建立真实平台交付证据体系：交付目标随任务保存，实机状态只能由客户端版本、测试日期、成品哈希与发送、接收播放、转发、下载、重开全链路证据推导；规则推断和过期证据不会冒充已验证。

5.7.10 新增素材恢复与原位重连：恢复上一次任务时保留素材身份和编辑设置；文件移动或丢失后可在素材卡直接重连，合并列表同步更新；媒体读取和时间线关键帧可以独立重试，旧路径的晚到结果不会污染新素材。

5.7.9 新增失败恢复中心：关键交付失败会提供中文分类、处理建议和安全重试入口；候选、批量与全部格式仅重试失败项，分享诊断前自动隐藏本地路径、任务编号等敏感信息。

5.7.8 增加真实桌面后端压力验收：Fast、Target 与质量检测可并行硬取消并清理各自临时输出；不同 FFmpeg 任务相互隔离；首次并发能力发现采用单次初始化，取消不会缓存不完整运行时能力。

5.7.7 拆分“快速素材探测”和“精确交付验收”：长素材导入保持快速，内部 NUT、Animated WebP 与最终成品恢复精确计帧，避免将元数据估算值用于严格时间线校验。

5.7.6 将任务停止升级为真实硬取消：终止活动编码进程树、清理临时输出，并通过取消/提交状态机保证已接受取消的任务不会发布最终文件。

5.7.5 新增长素材与任务控制：快速媒体探测、快速缩略图定位、负载分级、自适应采样和并发，以及可停止继续派发并隔离晚到结果的任务状态。

5.7.4 新增输出信心与决策层：基于真实体积、质量实测、目标命中、透明状态和编码警告给出“放心交付 / 建议检查 / 需要调整”，并支持更清晰、更小、更流畅的一键重生成。

5.7.3 新增桌面端输出稳定性防护：生成、合并、动态海报与录屏会提前验证输出目录；录屏最终发布失败时保留可重试会话，并给出可执行的中文提示。

5.7.2 新增上一次任务自动保存与恢复：重新打开后可以恢复素材、裁剪、时间范围、删帧、标注与输出参数，也可以明确放弃旧任务。

5.7.1 新增独立动态海报工作台：支持静态底板、标题信息、两个局部动态窗口，
并可一次交付完整 GIF 和轻量 Animated WebP。

5.7.0 建立 ROI readiness 契约：优先识别 Alpha 精确掩码、无模型时序差分和
内容显著性基线，在结果报告中说明是否值得进行 SAM 增量 A/B。当前版本不捆绑、
不下载也不声称执行 SAM 模型。

5.6.0 将 Target 改为 Fast 实测体积的 80% / 60% / 40% tight-cap，复用 Fast
基线并输出可达/不可达；同源经济性表实测比较 GIF、WebP、APNG、AVIF、MP4、
WebM 的体积、质量、耗时和兼容性。

内部便携版使用 FFmpeg GPL shared build；通过公开分发门禁的便携版使用
独立审阅的 LGPL shared build。FFmpeg 的准确版本、构建参数、许可证、
来源和文件校验值见同目录的 `FFmpeg-BUILDINFO.txt`、
`FFmpeg-PROVENANCE.json`、`FFmpeg-SOURCE-INFO.txt` 与
`THIRD_PARTY_LICENSES/FFmpeg-LICENSE.txt`。

GIFP 的免费软件许可见 `LICENSE.txt`，全部第三方说明见
`THIRD_PARTY_NOTICES.md`。`DISTRIBUTION.json` 是当前软件包能否再分发的
权威状态；预览包不因包含开源声明而自动获得公开再分发资格。
